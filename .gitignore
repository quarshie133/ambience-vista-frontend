# Environment Variables
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Large media files — do NOT commit these to git
# Video files
*.MOV
*.mov
*.MP4
src/assets/*.mp4

# HEIC image files (iOS format, not web-compatible)
*.HEIC
*.heic

# Public folder videos — block most, but allow the hero video so Vercel can serve it
public/*.MOV
public/*.mov
# Allow only the hero video (46MB — under GitHub's 100MB hard limit)
!public/0_Paint_Painting.mp4
